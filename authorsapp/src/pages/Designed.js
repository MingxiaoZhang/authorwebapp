import Form from "../components/form";
import SearchId from "../components/searchId";

const Designed = () => {
    return (
        <div>
            <Form />
            <SearchId />
        </div>
    );
};

export default Designed;
